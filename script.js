// 1 criação do store 
const store = Redux.createStore(reducer);

// 2 criação do reducer 
const reducer = ( state = INITIAL_STATE ) => {
    switch ( action.type) {
        default:
            return state;
    }
}